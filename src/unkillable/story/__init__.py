"""Storyteller trio: Nightwatch Diary, Anomaly Copilot, Memory Palace.

A separate add-on module that reads the existing index (``IndexEntry``
records) and writes its own artifacts under ``storage/story/``.  The main
motion/detection/search/backup pipeline is untouched.
"""